/**
 * Calculate Average True Range (ATR)
 */

export function calculateATR(closes: number[], period: number = 14): number {
  // TODO: Implement ATR calculation
  // ATR is the moving average of true ranges
  return 0;
}

